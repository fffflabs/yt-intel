# Examples

This directory contains small curated artifacts that mirror the repository's output contracts without requiring the full local dataset.

Files:

- `single-video.json` shows the shape written by `npm run fetch`
- `channel-export.json` shows the shape written by `npm run export`

These samples are intentionally short and synthetic. They exist to help reviewers understand the data model quickly.
